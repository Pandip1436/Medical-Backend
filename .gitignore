node_modules
dist

# Keep environment variables out of version control.
# .env.example IS tracked — it's the template for new contributors.
.env
.env.*
!.env.example

/generated/prisma

# User-uploaded files (prescriptions, shared invoice/quotation PDFs, etc.)
/uploads

# Local dev runtime logs
backend-startup.log
*.log

# One-time migration re-baseline (2026-06): the 46 pre-baseline migrations,
# archived locally for reference only. Must NOT be committed — the canonical
# history is now prisma/migrations/0_init.
/prisma/migrations_archive/
